                    BOOLEAN (True / False)
                             │
        ┌────────────────────┼────────────────────┐
        │                    │                    │
   Creation            Boolean Operators     Comparison Operators
        │                    │                    │
   ┌────┴────┐        ┌──────┼──────┐       ┌─────┼─────┐
   │         │        │      │      │       │     │     │
True/False  bool()   and    or     not     ==    !=    >
                                      │      │     │
                                      <     >=    <=

                             │
                             ▼
                   Conditional Statements (if)
                             │
         ┌──────────────┬──────────────┬──────────────┐
         │              │              │
        if           if-else       if-elif-else
                             │
                             ▼
                    Boolean in Loops
                             │
                  ┌──────────┴──────────┐
                  │                     │
                while                 for
                  │                     │
        condition must be True     filtering using if

                             │
                             ▼
                    Truthy / Falsy Values
                             │
        ┌────────────────────┼────────────────────┐
        │                    │                    │
     Truthy              Falsy              Examples
 (evaluates True)   (evaluates False)  
        │                    │
   Non-zero numbers      0, None
   Non-empty list        "", [], {}
   True                  False

                             │
                             ▼
                    Boolean Functions Usage
                             │
        ┌────────────────────┼────────────────────┐
        │                    │                    │
     any()                all()               in / not in
 (any True?)         (all True?)          membership check