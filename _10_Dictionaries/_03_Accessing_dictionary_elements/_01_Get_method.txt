                            
                                             Accessing Dictionary in Python

1.  Using get() Method (Safer)
person = {"name": "John", "age": 20}

print(person.get("name"))   # John
print(person.get("marks"))  # None 

* ('none' is also a data type like boolean, list, integer etc.)

NOTE : No error if key is missing

****
~~ With default value:
print(person.get("marks", 0))   # Output: 0
(cause key named 'marks' does not exist)


Also ==> Input with Default Value (Recommended )

data = {"name": "John", "age": 20, "city": "Delhi"}

key = input("Enter key: ")

print(data.get(key, "Key not found"))
~ Output:
Input: name → John
Input: marks → Key not found