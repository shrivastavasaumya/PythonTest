
CLASS : -

1st parameter = 'cls'
classes can only call or access call attribute/methods, they cannot access instance attributes
Decorator = @classmethod 




 class laptop:
    storage_type = 'ssd'

    def __init__(self, RAM, storage):
        self.RAM = RAM
        self.srorage = storage

    @classmethod        ------> Decorator {should be written on the top}
    def get_storage_type(cls):
        print(f"storage type = {cls.storage_type}")

    def get_info(self):
        print(f"laptop has {self.RAM} RAM and {self.srorage} {self.storage_type}")

l1 = laptop('16gb', '512gb') 
l2 = laptop('8gb', '256gb')
laptop.get_storage_type()        # storage type = ssd = output
li.get_storage_type()            # storage type = ssd = output  [class can be called with object's name cause of its priority]

NOTE = but this is not the other way around, all objects name/methods/attributes cannot be called with class names
classes can only call call attribute/methods


Decorator - Decorator is a  function itself but it takes another function and Changes the behaviour of that function 
and than returns it back (that is why it is written at the top of class method to initate the changing process)
